# v0.4.0 群聊回复与番剧卡片验收记录

- 版本：v0.4.0
- 日期：2026-07-29
- 自动化状态：passed
- ACR、真实服务器与 QQ 群状态：external_gate

## 已通过

- 当前实现全量 PostgreSQL 测试：389 passed；
- Ruff format/check：通过；
- mypy：75 个源码文件通过；
- `uv lock --check`：通过；
- 生产 Compose 配置解析：通过；
- `git diff --check`：通过；
- CardDataAssembler：latest confirmed 快照、时区下一集、Mikan 来源标签与 NSFW 拒绝；
- PosterCache：HTTPS 逐跳重定向验证、格式、大小、像素、原子 manifest 与安全本地查找；
- PosterWarmup：Bangumi 下载或解码失败后尝试 AniList；
- 本机真实字体样卡：`1000 × 600` PNG 可解码；
- 实际 Linux 应用镜像 `anime-qqbot:v0.4.0-rc` 构建通过，镜像内 CJK 样卡通过；
- 容器内代表性首次渲染：0.066 s；
- 容器内代表性缓存命中：0.004 s；
- 容器内代表性渲染 RSS delta：约 23.4 MiB；
- 最终本地镜像摘要：
  `sha256:cd64edd0455b07a249d0817d5808229b90d07eae21f1c4f71f1011deb7815533`。

## 待完成外部门禁

- 2 GiB Compose 运行时 OOM/swap 观测；
- ACR 发布脚本前后 NapCat 指纹。

## 真实 QQ 测试群 external_gate

使用一个已有本地真实海报的唯一条目：

```text
/番剧 搜索 <唯一命中名称>
@机器人 详情 <名称>
@机器人 下一集 <名称>
```

确认只收到一条回复，图片为普通横图，海报完整且不拉伸，标题、放送时间和来源标签在
手机 QQ 与桌面 QQ 均可读。

验证以下场景保持文本：

```text
今日番剧
本周番剧
/番剧 季度
/番剧 我的订阅
/番剧 帮助
```

最后选择没有本地海报的条目，确认只收到包含所有可用语义字段的结构化文本，没有
占位图。

## 发布门禁

服务器继续使用：

```bash
cd /opt/anime-qqbot
./scripts/deploy-acr.sh
```

日常发布不使用 `--refresh-vendors`。发布前后记录 NapCat 容器 ID 与 `StartedAt`，脚本
必须输出：

```text
NapCat restart detected: no
```

上述 Docker、2 GiB 和 QQ 外部门禁完成前，只能称为“自动化候选版”，不能宣称生产验收
完成。
