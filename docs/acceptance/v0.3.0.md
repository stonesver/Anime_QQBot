# v0.3.0 安全交互与控制台验收记录

- 版本：v0.3.0
- 日期：2026-07-29
- 自动化状态：通过
- 真实服务器与 QQ 群状态：待部署者 canary

## 已通过的发布门

本地使用 PostgreSQL 测试实例完成：

```bash
.venv/bin/ruff format --check .
.venv/bin/ruff check .
.venv/bin/mypy src astrbot_plugin_anime_tracking/anime_tracking_plugin
TEST_DATABASE_URL=postgresql+asyncpg://anime:anime@127.0.0.1:55432/anime_test \
  .venv/bin/pytest -q
docker compose config --quiet
```

最终结果：

- Pytest：332 passed；
- Ruff format/check：通过；
- mypy：73 个源码文件通过；
- Alembic：空库到 `0012` head、head 到 base 再回 head、历史快照前向升级和
  旧索引降级重建均通过；
- Plugin Page：桌面 1280 px 与手机 390 px 真浏览器验证通过，无横向溢出；
- 页面静态资源不依赖 CDN 或前端构建链；
- `git diff --check`：通过。

本机没有 `uv` 命令，本次没有重新生成依赖解析；项目依赖集合未变化，`uv.lock`
只同步了本地包版本 `0.3.0`。

## 验收覆盖

- `/番剧` 固定命令继续可用；
- @机器人自然入口、严格短命令和普通聊天防误触；
- 候选结果按群、用户和五分钟时效隔离；
- 群策略、安静时段、主动提醒开关和乐观锁；
- 全局、同群、同用户、主动提醒发送节流；
- 暂停群不会阻塞其他群的 Outbox；
- 临时失败任务回到 pending 并延迟重试；
- 账号下线和限流分类、持久化暂停与熔断；
- AstrBot Dashboard Plugin Page 的只读总览与受开关保护的写操作；
- 管理任务、通知操作和审计事件；
- 管理 API 不返回 UMO、通知 payload、Token、密码或完整 QQ 用户标识；
- QQ 群主和群管理员不获得机器人配置权限，只有 AstrBot `admin` 角色可执行群内
  设置操作。

## 真实测试群 canary

部署后按顺序记录：

| 步骤 | 结果 | 证据/备注 |
|---|---|---|
| migration 显示 `0012_interaction_delivery_admin (head)` | ______ | |
| AstrBot 加载 `anime_tracking 0.3.0` | ______ | |
| NapCat 登录且 OneBot 反向 WebSocket 已连接 | ______ | |
| `/番剧 帮助` 正常返回 | ______ | |
| 开启 `interaction_gateway_enabled` 后 @机器人查询正常 | ______ | |
| 未开启短命令时普通聊天和 `今日番剧` 不触发 | ______ | |
| 仅 AstrBot 管理员可以开启本群短命令 | ______ | |
| QQ 群主/管理员无法执行机器人设置 | ______ | |
| 控制台可经 SSH 隧道打开且不经 Nginx 暴露 | ______ | |
| 开启发送治理后查询与主动提醒不刷屏 | ______ | |
| 开播和 Mikan 更新各收到一次且格式正确 | ______ | |
| 模拟断线后熔断，恢复登录并人工解除成功 | ______ | |

自动化结果不能替代 QQ 登录风控、真实 OneBot 事件角色、上游数据和群投递验证。
上述 canary 完成前，发布状态保持为“代码可部署，生产待验收”。
