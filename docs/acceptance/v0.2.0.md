# v0.2.0 AstrBot 多源追番验收记录

- 版本：v0.2.0
- 日期：2026-07-28
- 自动化状态：通过
- 真实群状态：待部署者 canary

## 自动化验收范围

最终发布前必须全部通过：

```bash
UV_CACHE_DIR=.uv-cache .venv/bin/uv lock --check
UV_CACHE_DIR=.uv-cache .venv/bin/uv sync --frozen
.venv/bin/ruff format --check .
.venv/bin/ruff check .
.venv/bin/mypy src
TEST_DATABASE_URL=postgresql+asyncpg://anime:anime@127.0.0.1:55432/anime_test \
DATABASE_URL=postgresql+asyncpg://anime:anime@127.0.0.1:55432/anime_test \
  .venv/bin/pytest
docker compose config --quiet
docker compose build worker astrbot
```

2026-07-28 实测结果：

- `pytest`：264 passed；
- Ruff format/check：通过；
- mypy（`src`）：48 个源码文件通过；
- Alembic：空库升级、head 降级至 base、再次升级均通过；
- Worker/AstrBot 镜像：固定版本真实构建通过；
- NapCat：固定镜像入口、路径和最终 OneBot JSON 验证通过；
- 隔离五单元 Compose：PostgreSQL、Worker、AstrBot、NapCat 均 healthy，
  migrate 退出码 0；
- AstrBot 日志确认 `anime_tracking 0.2.0` 加载并执行
  `on_astrbot_loaded` 启动钩子。

覆盖项目包括：

- 固定群命令、季度/今日/本周/搜索/详情查询；
- 订阅、取消订阅、订阅筛选和内部 Anime ID；
- Bangumi/AniList 同步、匹配与投影；
- 开播任务、持久化 Outbox、并发领取、重试和过期；
- Mikan 条件请求、安全 XML、发布去重、十分钟持久化聚合和筛选；
- 迁移从空库到 head、降级后再升级；
- AstrBot 插件生命周期、消费者心跳和消息投递；
- 固定镜像、端口、挂载、Token 及部署/恢复脚本静态约束。

## 真实测试群 canary

部署者按顺序填写：

| 步骤 | 结果 | 证据/备注 |
|---|---|---|
| NapCat 登录专用 QQ 小号 | ______ | |
| AstrBot OneBot 反向 WS 已连接 | ______ | |
| `/番剧 帮助` 返回命令列表 | ______ | |
| `/番剧 搜索 <关键词>` 返回条目 | ______ | |
| `/番剧 订阅 <Anime ID>` 成功 | ______ | |
| 精确开播时收到群提醒并正确 @ | ______ | |
| Mikan 更新在十分钟窗口后聚合提醒 | ______ | |
| 重启 Worker/AstrBot 后无重复通知 | ______ | |

自动化验收不能替代 QQ 登录风控、真实群权限和上游真实数据验证。上述 canary 全部通过
后，才可将部署状态标记为“可用”。
